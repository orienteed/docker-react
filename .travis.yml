sudo: required
services:
  - docker

before_install:
  - docker build -t fschettini/docker-react -f Dockerfile.dev .      

script:
  -  docker run fschettini/docker-react npm run test -- --coverage 
